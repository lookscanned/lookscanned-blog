---
import CategoriesIndex from "../../components/CategoriesIndex.astro";
import { localeStaticPaths } from "../../i18n";

export const getStaticPaths = localeStaticPaths;

const { locale } = Astro.props;
---

<CategoriesIndex locale={locale} />
