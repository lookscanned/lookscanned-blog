---
import SectionList from "../../../components/SectionList.astro";
import { localeStaticPaths } from "../../../i18n";

export const getStaticPaths = localeStaticPaths;

const { locale } = Astro.props;
---

<SectionList locale={locale} />
