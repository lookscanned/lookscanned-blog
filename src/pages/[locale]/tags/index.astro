---
import TagsIndex from "../../../components/TagsIndex.astro";
import { localeStaticPaths } from "../../../i18n";

export const getStaticPaths = localeStaticPaths;

const { locale } = Astro.props;
---

<TagsIndex locale={locale} />
